/**
 * Simplified Mustache implementation for Anki.
 *
 * Has only one public class: Template. No exceptions, no compiler. Just a regex-based Template. Instantiate
 * with a String and .execute(with a Map<String, String>).
 */
package sh.backsla.mustache;
